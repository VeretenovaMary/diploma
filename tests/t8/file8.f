! line cannot contain more than _lineLen characters
! if _lineLen <= 72, then nothing will be printed. if 72 < _lineLen <= 132 then just one string will be printed
      PROGRAM
                                                                        PRINT *, "132 characters in _lineLen"
                                                                                                                                    PRINT *, "won't be printed anyway"
      END