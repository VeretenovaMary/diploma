! if line length is limited, then line cannot contain more than _lineLen characters
PROGRAM
                                                                                                                                    PRINT *, "if _lineLen <= 132 this won't be printed"
END